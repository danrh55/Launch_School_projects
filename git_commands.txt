git status
git --version
git config --global init.defaultBranch main
git config --global user.name "Eric Cho"
git config --global user.email "ekc7590@gmail.com"
--system 
# create repository by adding the directory you are in with .git directory. delete .git to revert back to normal not a repo. 
git init 
ls -a (shows hidden files and dir)
ls --help

nano

git branch -d newBranchName (deletes branch if fully merged with main. -D without merging)
git merge (merging another branch into the current checkouted branch)

git add file1.rb
git add --all (make sure logical group of changes)

git log (log of changes)
git branch (shows us all the branches including main)
git branch newBranchName (creates branch but doesn't change branch)
git branch -b newBranchName(creates and switch)
git checkout newBranchName (changes branch)

git commit -m "message flag"

git push -u origin(alias for remote repo, usually githubgi) main (main branch)
# tells us all the remote repos local knows about
git remote 